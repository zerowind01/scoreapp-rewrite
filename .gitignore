# ============================================================
# 本仓库只提交「独立重写」的成果。
# 原始 APK 与 jadx 反编译产物属于第三方应用的版权材料，
# 仅作为本地分析输入，一律不入库。
# ============================================================

# ---- 原始 APK（第三方应用）----
base.apk

# ---- jadx 反编译产物（本地分析用）----
source_recover/
source_recover_verbose/
source_recover_check/
decoded_resources/

# ---- 分析工具与运行日志 ----
# 注意：不能直接写 `tools/`，那样 git 不会进入该目录，后面的 `!` 例外全部失效。
# 必须写成 `tools/*` 并把要保留的文件放在例外之前逐个放开。
tools/*
# 例外：这是原型内置 PDF 的生成脚本，属于交付物的一部分，必须入库
!tools/make_proto_pdf.py
# 例外：APK 验收脚本（解析 dex 字符串池核对符号是否真打进包），可复用
!tools/check_dex_symbols.py
# 生成出来的 base64 是中间产物，不入库（跑脚本即可重现）
tools/proto_pdf_b64.txt
jadx_*.log

# ---- 本地验证产物（截图 / 临时补丁 / dex 解包目录）----
.tmpdex/
proto_*.png
scoreapp-fix.*
logo-preview.svg

# ---- 本地工作笔记（AI 助手记忆目录，含逆向分析过程记录）----
.workbuddy-ai/

# ---- 构建产物 ----
*.apk
*.aab
build/
.gradle/
.kotlin/
local.properties
captures/

# ---- IDE / 系统 ----
.idea/
*.iml
.DS_Store
Thumbs.db
