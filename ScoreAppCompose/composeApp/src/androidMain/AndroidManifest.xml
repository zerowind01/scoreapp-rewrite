<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        相册读取走系统的照片选择器（Photo Picker），**主路径不需要任何权限**：
        选择器由系统进程托管，只对用户勾选的那几张授予临时读权限。

        这里仍然声明权限，是为了兼容两条边缘路径：
          · Android 14+ 的「部分照片访问」——用 READ_MEDIA_VISUAL_USER_SELECTED 表达
            「用户只授权了选中的几张」，不声明的话系统会把它折叠成完全拒绝；
          · 部分定制 ROM 上 Photo Picker 缺失、回退到旧相册界面的情况。
        申请入口已在 MainActivity 中整体删除，因此这些声明不会触发权限弹窗。
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScoreApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|fontScale"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.ScoreApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            分享 PDF 附件需要把 filesDir 下的文件暴露成一个可授权的 content:// URI。
            exported=false + grantUriPermissions=true 是标准组合：平时外部无法访问，
            仅在 share 时按次授权给接收方。
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.scoreapp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
